
// O serviço de IA foi removido do projeto.
// Este arquivo é mantido vazio para evitar erros de importação caso existam referências antigas.

export const generateMessage = async () => {
  return "";
};

export const analyzeDay = async () => {
    return "";
}
